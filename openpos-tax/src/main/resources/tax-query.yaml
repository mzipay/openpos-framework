---
queries:
-
  name: taxJurisdictionLookup
  where: geo_code = ${geoCode}
-  
  name: taxGroupRuleLookup
  where: authority_id = ${authorityId}
-
  name: taxRateRuleLookup
  where: authority_id = ${authorityID}
-
  name: taxRateRuleLookup2
  where: (group_id = ${groupId}
      AND authority_id = ${authorityId})