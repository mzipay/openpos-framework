apply from: '../jumppos-server/common.gradle'

dependencies {
    compile "org.hibernate:hibernate-entitymanager:5.2.10.Final"
    compile group: 'org.springframework', name: 'spring-context', version: "${springVersion}"
}
